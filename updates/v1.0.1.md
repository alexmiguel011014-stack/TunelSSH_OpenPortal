# v1.0.1 — Conexao Direta + Menu Atualizacoes + Janela de Progresso

**Data:** 2026-07-30

## O que foi implementado

- Card "Conectar por IP" no Dashboard — conexao direta sem configuracao previa
- Menu "Atualizacoes" na barra do Electron com atalho Ctrl+U
- Verificacao diaria automatica de atualizacoes (24h)
- Janela de progresso com barra e log durante download de atualizacao
- Fechamento automatico do app apos download + restart
- Mensagem "Nenhuma atualizacao disponivel" quando nao ha updates
- Tratamento de erros com dialogo informativo
- Corrigido bug: `setActiveMachine` nao estava exposto no contexto (causava "ee is not a function")
- Corrigido bug: `activeMachine` aceita objeto ou string ID
- Portugues corrigido em todo o app (Atualizacoes, conexoes, maquina, etc)

## Arquivos criados/modificados

| Arquivo | Tipo | Descricao |
|---------|------|-----------|
| `resources/update-progress.html` | Novo | Janela de progresso com barra e log |
| `src/main/main.js` | Modificado | Menu Atualizacoes + janela de progresso + auto-check + IPC checkUpdate + portugues |
| `src/renderer/src/App.jsx` | Modificado | activeMachine aceita objeto/string + setActiveMachine no contexto |
| `src/renderer/src/components/Dashboard.jsx` | Modificado | Card "Conectar por IP" + portugues corrigido |

## Como atualizar

Baixe o instalador na Release do GitHub ou aguarde o auto-update.
